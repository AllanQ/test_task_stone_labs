$('.question').html("<%= j render 'questions/shared_show/question' %>");
$(function() {
  var answer_text = $('#answer_text').val();
  $('#answer_text').focus().val('').val(answer_text);
});
form_send();
