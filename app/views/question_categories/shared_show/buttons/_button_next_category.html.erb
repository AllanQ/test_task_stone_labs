<% if next_category = @category.next_category(params[:type_questions],
                                              current_user.id) %>
  <%= link_to 'Next category', question_category_path(next_category,
                                       type_questions: params[:type_questions]),
                                class: 'btn btn-success',
                                remote: true if next_category %>
<% end %>
