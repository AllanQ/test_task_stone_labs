var element = document.getElementById("<%= @question_category.id  + 10000 %>");
$ ("#<%= @question_category.id  + 10000  %>").hide(500);
setTimeout(continueExecution, 500);

function continueExecution()
{
element.remove();
}
