<% if can? :destroy, category %>
  <td><%= link_to 'Destroy', question_category_path(category),
                  method: :delete,
                  data: { confirm: 'Are you sure?' },
                  class: 'btn btn-mini btn-danger',
                  remote: true %></td>
<% end %>